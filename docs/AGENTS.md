# AGENTS.md
## Setup
- Run: `bash .codex/setup.sh`
## Build
- Run: `dotnet --info`
- Run: `dotnet restore`
- Run: `dotnet build -c Release --nologo`
## Test
- Run: `dotnet test -c Release --no-build --nologo`
