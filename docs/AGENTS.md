# AGENTS.md
## Setup
- Run: `bash .codex/setup.sh`
## Build
- Run: `dotnet --info`
- Run: `dotnet restore`
- Run: `dotnet build -c Debug --nologo`
## Test
- Run: `dotnet run -c Debug --no-build --nologo`
